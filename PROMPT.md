# TASK: Pitch Deck PDF Upload & Parsing Engine for AI Scoring

Act as a Principal Backend & AI Engineer.

First, read `CONTEXT.md`, `TASK.md`, `backend/routers/startup.py`, and `backend/workers/ai_scoring_worker.py`.

---

## 1. DEPENDENCY SETUP
Add `pypdf` to `backend/requirements.txt`:
```bash
pip install pypdf python-multipart